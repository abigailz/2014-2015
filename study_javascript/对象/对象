（1）javascript的函数和对象既有对象的特征也有数组的特征，
    一种可以任意伸缩的名称值对儿的集合，在需要的时候，
    自由的选择用对象还是数组来解释和处理问题

（2）var anObject = new aFunction()
    第一步：建立一个新对象，
    第二步：将该对象的内置的原型对象设置为构造函数prototype引用的那个原型对象，
    第三步：将该对象的作为this参数调用该构造函数

（3）BOM（bower  object  model）浏览器对象模型
简单的数据类型：
（1） undefined 未定义   typeof（undefined） 结果是undefined
（2） null 空值(空对象) typeof(null)返回object，但null不是object，具体的null值的变量也不是object
（3） boolean 布尔值
（4） Number  typeof(NaN)和typeof(infinity)都是Number ，NaN参与任何数值计算的结果都是NaN ，NaN！=NaN，infinity/infinity = NaN
（5） String

除了undefined，null，“”，0，false，转换为逻辑值的时候是false，其他任何东西转换为逻辑值的时候都是true，
这五个值当中除了undefined == null以外其他的都不相等

（6） object类型

常见的属性以及方法：
         （1）constructor保存用于创建当前对象的函数
         （2）hasOwnProperty（propertyName）检测给定属性在当前对象实例中是否存在
         （3）isPrototypeOf（object）检测传入的对象是否是另一个对象的原型
         （4）propertyIsEnumerable（propertyName）检测给定属性名是否是当前对象能用for-in语句枚举的
         （5）toLocaleString（）返回对象的字符串表示，转换为本地格式字符串并返回
         （6）toString（）返回对象的字符串表示
         （7）valueOf（）返回对象的字符串，数值或布尔值表示；通常与toString（）的值相同
         （8）trim（）清除前置以及后缀的空格
         （9）trimLeft（）清除前置空格
         （10）trimRight（）清除后置空格
         （11）localeCompare（）用本地特定的顺序比较两个字符串，例如：string.localeCompare(string2);
         （12）toUpperCase()转为大写
         （13）toLocaleUpperCase（）转为大写（按照本地数据格式转换，每个地区的大小写规定可能不一样）
         （14）toLowerCase（）转小写
         （15）toLocaleLowerCase（）转小写
         （16）escape（）字符串常规编码（例如：escape(str)返回一个包含str内容的字符串值(Unicode格式)所有空格，标点，重音符号以及任何其他非ASCLL字符都用%XX编码替换，期中XX等于表示该字符的十六进制数，例如空格返回%20）;
         （17）unescape（）字符串常规解码
         （18）encodeURI()uri字符串编码
         （19）decodeURI()uri字符串解码
         （20）encodeURIComponent()uri组件编码，与encodeURI不同的是可以对uri中具有特殊意义的字符进行编码
         （21）decodeURIComponent()uri组件解码
